# Supabase — pegar em: painel do projeto > Project Settings > API
# Copie este arquivo para .env.local e preencha.
# O .env.local nao vai para o GitHub (esta no .gitignore).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
